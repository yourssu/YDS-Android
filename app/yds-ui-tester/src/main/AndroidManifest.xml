<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Light.YourssuDesignSystem">
        <activity
            android:name=".ToolTipActivity"
            android:exported="true" />
        <activity
            android:name=".EditTextActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".TopBarActivity" />
        <activity android:name=".PlainButtonActivity" />
        <activity android:name=".SearchTextFieldActivity" />
        <activity android:name=".PasswordTextFieldActivity" />
        <activity android:name=".SuffixTextFieldActivity" />
        <activity android:name=".SimpleTextFieldActivity" />
        <activity android:name=".BadgeActivity" />
        <activity android:name=".ToggleActivity" />
        <activity android:name=".BoxButtonActivity" />
        <activity android:name=".DividerActivity" />
        <activity android:name=".DesignSystemListActivity" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>